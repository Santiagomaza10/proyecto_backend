import { Router } from "express";

/* ACA HAY QUE TRAER EL MANAGER DEL CART */

/* ACA HAY QUE INSTANCIAR LA CLASE DEL CARTMANAGER */

const router = Router();

/* A PARTIR DE ACA HAY QUE LLAMAR A LOS METODOS DEL CART */

export default router